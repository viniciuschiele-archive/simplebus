# Changelog

### Version 2.0.0 (next)
 - SimpleBus has been refactored breaking compatibility with old versions.
 - Support to incoming and outgoing messaging pipeline, these pipelines can be customized.
 - Message published or subscribed is no longer a dict, from now any message must be a class, there are two kind of messages, command and event.

### Version 1.0.1
 - Fixed bug in retry logic.
 - Moved retry logic to the transport level.
